#uncomment the below text in order to create a new table 
#     q = "create table logins(username  text, password text);"
#     cursor = connection.execute(q)
#     results = [line for line in cursor]
